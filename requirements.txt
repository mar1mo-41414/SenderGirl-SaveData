# No third-party packages are required to run this project (the
# Tkinter GUI included) -- Python 3.8+ standard library only.
# To build the .app / .exe, see requirements-dev.txt.
# (Comments here are kept ASCII-only: pip's requirements parser and
# some Windows tools mis-decode non-ASCII bytes under certain locales.)
